baseURL       = 'https://joe-mooring-test-4.web.app/'
locale   = 'en-US'
title         = 'Hosting Test - Firebase'

enableGitInfo = true

[caches]
  _merge = 'deep'

[cascade]
  _merge = 'deep'

[markup]
  _merge = 'deep'

[security]
  _merge = 'deep'

[[module.imports]]
  path = 'github.com/jmooring/hugo-module-feature-test'
